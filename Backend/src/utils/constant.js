export const Option = {
  httpOnly: true,
  secure: true,
};

export const PROMPT="write a message which is less than two line indicating that  the user you wanted to send message they are unavailable or busy right now and cannot receive messages";